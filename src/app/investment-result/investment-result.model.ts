export interface investmentResultModel {
    year: number;
    interest: number;
    valueEndOfYear: any; // Replace `any` with a more specific type if known
    annualInvestment: any; // Replace `any` with a more specific type if known
    totalInterest: number;
    totalAmountInvested: any; // Replace `any` with a more specific type if known
  }